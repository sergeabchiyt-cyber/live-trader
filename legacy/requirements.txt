websocket-client>=1.6.0
# The live server itself is pure stdlib except websocket-client, which powers
# the default WebSocket (push) market-data feed. `pandas` is needed only for
# the optional local parity check (live/validate.py).
